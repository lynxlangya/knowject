# Knowject Codex project configuration.
# `.codex/` is configuration + compatibility space only.
# Primary docs live in `docs/`; derived exports live in `docs/exports/`.
# Project skills live in `.agents/skills/`.

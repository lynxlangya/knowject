# Compatibility Stub

`docs/handoff/history/codex-workspace-migration.md` 是迁移手册的历史归档副本，不是当前治理的主入口。
`.codex/MIGRATION.md` 不再是主维护入口，仅保留兼容说明。
`.codex/docs/*` 可按兼容 stub 指引迁移到 `docs/*`。
`docs/exports/*` 是当前 live 导出层，ChatGPT Projects 导出包 live 根目录为 `docs/exports/chatgpt-projects/*`；`.codex/packs/chatgpt-projects/README.md` 仅保留 compatibility shell。
`.agents/skills/*` 是当前 Skill-root live 目录；`.codex/skills/README.md` 仅保留 compatibility stub 入口说明。

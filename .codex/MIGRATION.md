# Compatibility Stub

`docs/handoff/history/codex-workspace-migration.md` 是迁移手册的历史归档副本，不是当前治理的主入口。
`.codex/MIGRATION.md` 不再是主维护入口，仅保留兼容说明。
`.codex/docs/*` 可按兼容 stub 指引迁移到 `docs/*`。
`docs/exports/*` 与 `.agents/skills/*` 目前是迁移目标：Task 4 前导出包 live 入口仍是 `.codex/packs/chatgpt-projects/*`，Task 3 前 Skill-root live 兼容入口仍是 `.codex/skills/README.md`。

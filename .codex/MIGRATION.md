# Compatibility Stub

`docs/handoff/history/codex-workspace-migration.md` 是迁移手册的历史归档副本，不是当前治理的主入口。
`.codex/MIGRATION.md` 不再是主维护入口，仅保留兼容说明。
`.codex/docs/*` 可按兼容 stub 指引迁移到 `docs/*`。
`docs/exports/*` 仍是导出层迁移目标（Task 4 前导出包 live 入口仍是 `.codex/packs/chatgpt-projects/*`）；`.agents/skills/*` 已是当前 Skill-root live 目录，`.codex/skills/README.md` 仅保留兼容入口说明。

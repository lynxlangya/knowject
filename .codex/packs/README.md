# Compatibility Stub

`.codex/packs/` 不再是主导出目录。
`docs/exports/` 是正在建立的导出层目标根目录（对应 `docs/exports/*`）。
当前 ChatGPT Projects 导出包内容仍在 `.codex/packs/chatgpt-projects/*`，Task 4 前请继续使用该 live 入口。
Task 4 落地后，再统一切换 active 导出包引用到 `docs/exports/*`。

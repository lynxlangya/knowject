# Compatibility Stub

`.codex/packs/` 不再是主导出目录。
`docs/exports/` 是当前 live 导出层根目录（对应 `docs/exports/*`）。
ChatGPT Projects 导出包 live 根目录为 `docs/exports/chatgpt-projects/*`。
`.codex/packs/chatgpt-projects/README.md` 仅保留 compatibility shell，避免历史入口失联。

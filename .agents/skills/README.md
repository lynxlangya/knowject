# Repo Skill Root

`.agents/skills/` 当前是 Knowject 项目级 Skills 的 live 根目录。

当前目录下技能均为 Knowject 项目私有 Skills，不作为跨项目全局技能分发目录。

后续新增仓库级 Skills 统一落在 `.agents/skills/<skill>/`（至少包含 `SKILL.md`，并可按需同目录放置 `agents/`、脚本或资产文件）。

说明：本次仅完成 skill 层迁移；仓库其他根入口/治理文档仍可能在后续任务继续收敛。
兼容入口见 `.codex/skills/README.md`。

openapi: 3.0.3
info:
  title: Invalid Property Fixture
  version: "1.0.0"
paths:
  /users:
    get:
      operationId: listUsers
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                required: ["display-name"]
                properties:
                  display-name:
                    type: string
                  user id:
                    type: string
                  ok_name:
                    type: integer

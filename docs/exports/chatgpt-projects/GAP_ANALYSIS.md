# GAP_ANALYSIS（Derived）

本文件是导出副本，派生自 `docs/roadmap/*`、`docs/plans/*` 与 `AGENTS.md`。

- 当前差距管理原则：先保证事实源集中在 `docs/*`，再做对外导出。
- 导出层策略：仅保留高信号最小集合，不镜像周任务或专项契约文档。
- 迁移结论：`.codex/packs/chatgpt-projects/*` 降级为兼容壳，活跃导出入口为 `docs/exports/chatgpt-projects/*`。

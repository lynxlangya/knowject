# PROJECT_BRIEF（Derived）

本文件是导出副本，派生自 `AGENTS.md` 与 `docs/current/architecture.md`。

Knowject 是一个面向团队项目协作的知识平台：前端在 `apps/platform`，主后端在 `apps/api`，索引服务在 `apps/indexer-py`。文档主源是 `docs/`，导出层 `docs/exports/` 仅用于外部模型上传，不承担事实源职责。

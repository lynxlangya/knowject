# docs/exports/

`docs/exports/*` contains derived artifacts only and is never a source-of-truth surface.

## Sync Rules

- Update `docs/*` first, then update `docs/exports/*`.
- If content conflicts, `docs/*` always wins.
